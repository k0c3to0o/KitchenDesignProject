@model IEnumerable<KitchenDesignProject.Areas.Administration.Models.ProductModel>

<p>
    @Html.ActionLink("Create New", "Create")
</p>

@*@using (Html.BeginForm("Index", "Product", FormMethod.Post))
{
    @Html.DropDownList("lang", new SelectList(new[] { new KeyValuePair<string, string>("Bulgarian", "bg-BG"), new KeyValuePair<string, string>("Russian", "ru-RU"), new KeyValuePair<string, string>("English", "en-US") }, "Value", "Key", ViewBag.Lang), new { onchange = "this.form.submit();", id = "lang", @class = "form-control" })
}*@

<table class="table">
    <tr>

        <th>
            @Html.DisplayNameFor(model => model.CategoryId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TitleBg)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DescriptionBg)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.LangCode)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.CreatedDate)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.AuthorName)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.CategoryId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TitleBg)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DescriptionBg)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.LangCode)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CreatedDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.AuthorName)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.Id }) |
                @*@Html.ActionLink("Details", "Details", new { id = item.Id }) |*@
                @Html.ActionLink("Delete", "Delete", new { id = item.Id })
            </td>
        </tr>
    }

</table>
